# Agent Notes for Multiloader Docs

This directory is a reusable migration packet, not CarryBabyAnimals-only implementation code.

- Keep packet files practical and copyable for Tyler's other mod repos.
- Use placeholders such as `<repo root>` when a value must be replaced in each target repo.
- Put CarryBabyAnimals-specific facts in the formal spec or touchpoint inventory, not in generic checklist wording.
- Do not add broad Superpowers workflow rules here; this directory only documents multiloader migration ownership and verification guidance.
